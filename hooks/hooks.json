{
  "description": "Concierge 秘书层 — 每次会话开始时注入双向契约（输入意图澄清 + 输出认知友好）",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/session-start.sh\""
          }
        ]
      }
    ]
  }
}
